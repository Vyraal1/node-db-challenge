module.exports = {
  intToBoolean,
  booleanToInt
};

// file structure based on first week's Sprint
function intToBoolean(int) {
  return int ? true : false;
}

function booleanToInt(bool) {
  return bool ? 1 : 0;
}
